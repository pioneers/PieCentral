Config file: Devices: field names

->Runtime Call
con = new DeviceContext()
hibike = new Hibike(con)
con.sub(dev)
con.getSub(dev)


class DeviceContext:
contextData = {uid: ({param: (value, timestamp)}, delay, timestamp)}

getData(uid, param):
    returns (value, timestamp); None if not Sub
getDelay(uid):
    returns (delay, timestamp); None if not Sub

->These return immediately, runtime must check if the value has been updated
subToDevices(list(uid, delay)):
subDevice(uid):
writeValue(uid, param, value):
